---
import { slugify } from '@/utils'

const { name, label, divclass } = Astro.props
---

<section
  id={slugify(name)}
  aria-labelledby={slugify(label)}
  aria-label={label}
  class="relative px-6 mt-24 mb-12 md:px-12"
>
  <h2
    id={slugify(label)}
    class="mb-12 text-3xl font-semibold text-center text-violet-400 md:text-5xl font-title"
  >
    {name}
  </h2>
  <div class:list={["max-w-4xl mx-auto space-y-6", { [divclass]: divclass }]}>
    <slot />
  </div>
</section>
